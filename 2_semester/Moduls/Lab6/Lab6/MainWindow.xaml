<Window x:Class="Lab6.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab6"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" Margin="10" Height="400">
            <Label Background="AliceBlue" Content="Car Type"></Label>
            <ComboBox ItemsSource="{Binding CarTypes}" 
             DisplayMemberPath="Name" 
             SelectedValuePath="Id" 
             SelectedValue="{Binding Request.CarTypeId}" 
             Width="200" 
             Height="30"/>
            <Label Background="AliceBlue" Content="Client Full Name"></Label>
            <TextBox HorizontalAlignment="Center" Text="{Binding Request.ClientFullName}" Width="200" Height="30" FontSize="16"/>
            <Label Background="AliceBlue" Content="Client Phone Number"></Label>
            <TextBox Text="{Binding Request.ClientPhoneNumber}" Width="200" Height="30" FontSize="16" />
            <Label Background="AliceBlue" Content="Car Model"></Label>
            <TextBox Text="{Binding Request.CarModel}" Width="200" Height="30" FontSize="16" />
            <Label Background="AliceBlue" Content="Problem Description"></Label>
            <TextBox Text="{Binding Request.ProblemDescription}" Width="200" Height="30" FontSize="16"/>
            <Label Background="AliceBlue" Content="Master"></Label>
            <ComboBox ItemsSource="{Binding Masters}"
              DisplayMemberPath="Name" 
              SelectedValuePath="Id" 
              SelectedValue="{Binding Request.MasterId}" 
              Width="200" 
              Height="30" />
            <Button Content="Добавить" Width="100" Height="30" Click="AddRequest_Click" Margin="10"/>
        </StackPanel>
        <Button Content="Окно заявок" Click="btnOpenRequestWindow" HorizontalAlignment="Left" Margin="680,378,0,0" VerticalAlignment="Top" Height="39" Width="102"/>


    </Grid>
</Window>

<Window x:Class="exam.Views.AdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:exam.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="AdminView" Height="547" Width="660">
    <Grid Margin="0,0,0,15">
            
        <ScrollViewer Margin="0,0,10,0">
        <StackPanel Margin="125,10,125,10" Width="300">                         
            <Label Content="Вдача автомобиля" FontFamily="Arial Black" FontSize="20" HorizontalAlignment="Center"/>

            <Label Content="VIN" FontSize="18"/>
            <TextBox Text="{Binding VIN}" FontSize="18" Height="30" Margin="10"/>
            <Label Content="Имя" FontSize="18"/>
            <TextBox Text="{Binding Name}" FontSize="18" Height="30" Margin="10"/>
            <Label Content="Тип" FontSize="18"/> 
            <TextBox Text="{Binding Type}" FontSize="18" Height="30" Margin="10"/>
            <Label Content="Описание" FontSize="18"/>
            <TextBox Text="{Binding Description}" FontSize="18" Height="30" Margin="10"/> 
            <Label Content="Пользователь" FontSize="18"/>
            <ComboBox DisplayMemberPath="Name" SelectedValuePath="Id" SelectedItem="{Binding SelectedUser}" ItemsSource="{Binding Users}" FontSize="18" Height="30" Margin="10"/>

            <Button x:Name="btnAddCar" Content="Создать заявку" FontSize="18" Margin="10" Click="btnAddCar_Click"/>
            <Button x:Name="btnBack" Content="Отмена" FontSize="18" Margin="10"  Click="btnBack_Click"/>                 
        </StackPanel>       
      
      </ScrollViewer>
    </Grid>
</Window>

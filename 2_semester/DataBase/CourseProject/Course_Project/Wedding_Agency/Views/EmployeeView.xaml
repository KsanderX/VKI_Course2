<Window x:Class="Wedding_Agency.Views.EmployeeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wedding_Agency.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Список сотрудников агентства" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListView x:Name="EmployeeList" Grid.Row="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock Text="{Binding FullName, StringFormat='ФИО: {0}' }" FontSize="14"/>
                        <TextBlock Text="{Binding PositionTitle, StringFormat='Должность: {0}'}" FontSize="13"/>
                        <TextBlock Text="{Binding Email, StringFormat='Почта: {0}'}" FontSize="13"/>
                        <TextBlock Text="{Binding PhoneNumber, StringFormat='Номер телефона: {0}'}" FontSize="13"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">   
            <Button Grid.Row="1" Content="Назад" Width="100" Height="40" Margin="10" Click="btnBack_Click"/>
            <Button Content="Удалить сотрудника" Width="160" Height="40" Margin="10" Click="btnDeleteEmployee_Click"/>
            <Button Content="Редактировать" Width="160" Height="40" Margin="10" Click="btnEditEmployee_Click"/>
            <Button Grid.Row="1" Content="Добавить сотрудника" Width="160" Height="40" Click="btnAddEmployee_Click"/>

        </StackPanel>
    </Grid>
</Window>
